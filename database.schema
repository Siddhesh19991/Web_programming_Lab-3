create table user(firstname varchar(100), familyname varchar(100)  
      ,gender varchar(100),city varchar(100), country varchar(100),
       email varchar(100),
       password varchar(100),
       messages BLOB,
       primary key(email));

       
 create table token_data(token varchar(100),
       email varchar(100),
       primary key(email));
 